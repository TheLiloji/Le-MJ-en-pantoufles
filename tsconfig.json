{
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",              // utile pour que "paths" fonctionne
    "paths": { "@/*": ["./*"] }, // ton alias pour TS/IDE
    "resolveJsonModule": true    // pour importer les .json
  }
}
